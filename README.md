# api-performance-observatory
API Performance Observatory is a full-stack monitoring platform that tracks API metrics (latency, error rates, traffic) in real-time via an SDK, detects anomalies using per-endpoint statistical baselines, and provides a live dashboard with alerts, webhook notifications.
